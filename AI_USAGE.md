# AI 使用说明

本文说明本项目开发过程中使用了哪些 AI 工具、如何拆分任务、AI 出错时如何发现和修复，以及哪些关键部分选择使用人工设计的确定性代码。它不是功能清单，也不把计划或模拟测试写成真实生产事件。

## 1. 使用的 AI 工具

### Codex 编码代理

Codex 用于：

- 阅读现有 SQLite、FastAPI、React 和测试结构。
- 将需求拆为四个可独立验收的阶段。
- 修改后端、前端、测试和项目文档。
- 执行 `pytest`、Vite 构建、`git diff --check` 和本地接口检查。
- 操控本机 Google Chrome，验证桌面及 390px 移动视口、角色权限、日报下载和控制台状态。

用户要求规划、执行、验收分别使用指定模型，但当前任务接口不能在同一任务中真实切换模型。因此实际工作保留了三个阶段，未声称发生未执行的模型切换，也没有使用子代理。

### LangChain 与配置式 LLM

项目运行时使用 LangChain 的结构化输出能力，将自然语言转换为受约束的 `QueryPlan`。LLM 只负责意图解析和基于事实组织语言：

- 不执行模型生成的 SQL。
- 不直接读取或修改销售数据库。
- 不自行计算营业额、订单数、客单价或变化率。
- 未配置 API Key、模型调用失败或答案数字校验失败时，回退到本地规则和可信模板。

销售数字的唯一事实来源是 `data/clean/sales_clean.sqlite`。

### Chrome 浏览器自动化

浏览器自动化只用于最终 UI 验收，不用于替代后端测试。验收覆盖管理员和只读账号、日报生成和版本切换、CSV/XLSX/PDF 下载、异常提醒收起、AI 页面路由、响应式布局和控制台错误。

## 2. 如何拆分任务

任务不是一次性让 AI “把系统做完”，而是按风险和依赖拆成四阶段：

1. 数据基础与异常可见性：先证明数据是否可信，再展示异常。
2. 经营分析：在可信数据上实现周期比较、门店诊断和商品结构。
3. 日报与导出：将查询结果冻结为不可变快照，再从快照导出。
4. 权限与审计：最后统一注入门店范围并记录受保护操作。

每个阶段内部继续按同一顺序执行：

```text
确认数据口径
→ 设计 API 和持久化结构
→ 编写后端实现与独立 SQLite 复算测试
→ 接入 React 前端
→ 运行全量回归和生产构建
→ 使用真实 API 和 Chrome 验收
→ 更新进度与变更记录
```

这样拆分的原因是后续能力依赖前一阶段的业务口径。例如日报不能先于数据质量完成，否则可能把过期或异常数据包装成“正常日报”；权限必须覆盖所有已存在的查询和导出入口，因此放在最后统一验收。

### 真实 prompt 示例

阶段三实际使用的用户 prompt 核心内容如下：

> 按单个营业数据日期生成不可变日报版本。POST 负责生成，GET 只查询；CSV、XLSX、PDF 必须通过 report_id 导出已有版本。数据质量为 critical 时返回 409 DATA_QUALITY_CRITICAL，且不写入版本记录。阶段四保持未开始。

收到该 prompt 后，任务被拆为：

1. 检查应用库、质量服务、阶段二聚合接口和前端标签结构。
2. 设计 `daily_reports` 表、事务内版本递增和 SQLite 文件 SHA-256 数据版本。
3. 实现生成、列表、详情和导出四类接口。
4. 对 CSV、XLSX、PDF 分别使用结构化库生成并重新打开验证。
5. 增加运营日报标签、版本选择器和三种下载入口。
6. 测试 `critical` 不落库、旧快照不变化、版本号不冲突和门店筛选。
7. 全量回归后再进入 Chrome 验收；未通过浏览器验收时阶段三只记录为 90%，通过后才更新到 100%。

这类 prompt 提供了业务不变量和验收标准，AI 负责将其映射到仓库中的具体文件和测试，而不是自行改变产品范围。

## 3. AI 在哪里出错，如何发现和修复

### 数字编造风险：错误金额

数字真实性测试故意把模型回答替换为：

```text
净营业额为 ¥999,999.00。
```

这不是一次真实线上模型事故，而是主动注入的错误输出。独立 SQL 和后端 `facts` 得到的正确金额是 `85.0`。测试发现回答中的 `999,999` 不属于事实数字集合后，丢弃模型文本，改用后端模板回答，并将状态标记为 `answered_local`。

对应证据：`backend/tests/test_ai_truth_verification.py` 中的 `test_wrong_model_number_is_replaced_by_verified_template`。

### 数字编造风险：错误变化率

周期比较测试又故意让模型返回：

```text
变化率为 999%。
```

SQLite 事实是：当前周期 `123.0`、上一周期 `70.0`、绝对变化 `53.0`、变化率约 `75.7143%`。校验器识别到 `999` 不在允许的事实集合中，因此再次丢弃模型回答并使用模板。

对应证据：`backend/tests/test_ai_truth_verification.py` 中的 `test_compare_period_uses_two_sqlite_periods_and_deep_link`。

### 修复方法：回答前后都设边界

修复不是只在 prompt 中写“不要编造”，而是增加可执行约束：

1. LangChain 只输出白名单结构化查询计划。
2. 后端白名单查询函数从 SQLite 获取 `facts`。
3. `extract_business_numbers` 提取回答中的业务数字，并排除日期和门店编号。
4. `expected_business_numbers` 从 `facts` 构造允许出现的金额、数量、订单、绝对变化和变化率集合。
5. `validate_answer` 检查回答中的每个数字是否都能在事实集合中找到。
6. 校验失败时完全替换模型文本，而不是局部修补可疑数字。
7. 无数据、未知商品或不支持的利润问题不返回业务数字。

最终固定真值链路为：

```text
独立 SQL 结果 = API facts = 最终回答中的业务数字
```

当前报告 `data/reports/ai_answer_verification.json` 的固定用例一致率为 100%。报告中的 `fallback_used: true` 表示使用了本地可信回答，不应误解为远程 LLM 全部真实出错。

### 开发代理自身的错误

在整理本文档时，AI 最初只精确搜索 `AI_USAGE.md`，没有发现仓库已有但拼写为 `AI_USEAGE.md` 的文件，于是错误地新建了重复文档。用户指出已有文档后，通过宽范围文件名扫描发现问题。修复方式是迁移有效内容、删除错误拼写文件，并统一保留现在的 `AI_USAGE.md`。

Chrome 验收还发现了仅靠构建无法发现的运行时问题：登录后 React Hook 顺序崩溃、看板状态把 `/assistant` 重写为 `/`、切换账号后残留前一用户数据、只读用户仍显示受限操作入口。修复后重新执行全量测试、构建，并在新 Chrome 标签页复验控制台和两种视口。

## 4. 哪些部分选择自己写，为什么

这里的“自己写”指不把结果交给生成式模型自由生成，而是使用人工确定的业务规则、SQL、类型和模板实现。

### 业务指标与异常规则

营业额、去重订单数、客单价、退款占比、七日基线、30%/50% 阈值和连续无销售规则均由 SQL 与普通代码计算。原因是这些结果必须可复算、可测试，不能因 prompt 或模型版本变化而漂移。

### 权限和门店隔离

角色权限矩阵、门店范围条件和接口拒绝逻辑由后端确定性代码实现。前端隐藏按钮只改善体验，不作为安全边界。原因是权限判断必须在每次请求中稳定执行，不能交给模型推断。

### 日报版本和导出

日报版本号、不可变 JSON 快照、数据文件 SHA-256、`critical` 质量阻断以及 CSV/XLSX/PDF 生成均由事务和专用库完成。原因是历史日报必须可追溯，导出必须能够被标准解析器重新打开，不能让模型临时拼接文件内容。

### AI 事实校验和降级模板

允许出现的数字集合、答案校验和最终模板由代码实现。原因是如果仍让同一个模型判断自己是否编造数字，验证并不独立。确定性模板虽然语言变化较少，但在模型不可用或校验失败时仍能提供正确答案。

### 测试真值

关键验收数据使用独立 SQL 复算，而不是复用被测聚合函数。原因是复用同一实现只能证明两个调用得到相同结果，不能发现共同的计算错误。

AI 更适合承担需求整理、代码草拟、结构化意图解析和基于已验证事实的语言表达；金额计算、权限、审计、版本一致性和验收真值保留在确定性系统中。

## 5. 验收结果与限制

- 全量后端及清洗测试：`30 passed`。
- Vite 生产构建：通过。
- `git diff --check`：通过。
- 本机 Chrome 桌面与 390px 验收：通过，无横向溢出，新标签页控制台无错误。
- 日报 CSV、XLSX、PDF 请求：均为 HTTP 200。
- 当前接口没有实际完成阶段内模型切换，本说明不作相反声明。
- 本项目仍是本地 SQLite 演示环境；通过测试和本机验收不等同于已经完成生产部署。
