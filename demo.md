# AI 问答 Demo

本文展示一组真实执行过的连续问答。问题通过 `POST /api/v1/ai/query` 提交，以下回答和事实取自 `data/reports/ai_answer_verification.json`，不是为本文临时编写的示例数字。

## 对话演示

### 问题一：牛肉 poke 六月卖了多少钱？

AI 回答：

> 牛肉poke 在 2026-06 的净营业额为 ¥85.00，共售出 3 份，涉及 4 个订单。

系统解析出的查询条件：

```text
指标：净营业额
日期：2026-06-01 至 2026-06-30
商品：牛肉poke
门店：全部门店
```

数字核对：

| 回答数字 | SQLite/API facts | 结论 |
|---:|---:|---|
| 净营业额 ¥85.00 | 85.0 | 一致 |
| 销量 3 份 | 3 | 一致 |
| 订单数 4 | 4 | 一致 |

### 问题二：那五月呢？

AI 回答：

> 牛肉poke 在 2026-05 的净营业额为 ¥40.00，共售出 1 份，涉及 1 个订单。

这是上下文追问。系统只把日期改为 2026 年 5 月，并从上一问继承“牛肉poke”和“净营业额”，没有重新猜测商品。

```text
本轮修改：日期 2026-05-01 至 2026-05-31
继承条件：商品=牛肉poke，指标=净营业额
```

数字核对：

| 回答数字 | SQLite/API facts | 结论 |
|---:|---:|---|
| 净营业额 ¥40.00 | 40.0 | 一致 |
| 销量 1 份 | 1 | 一致 |
| 订单数 1 | 1 | 一致 |

### 问题三：换成三文鱼 poke 呢？

AI 回答：

> 三文鱼poke 在 2026-05 的净营业额为 ¥30.00，共售出 1 份，涉及 1 个订单。

这也是上下文追问。系统只将商品从“牛肉poke”替换为“三文鱼poke”，继续继承上一问的 2026 年 5 月日期和净营业额指标。

```text
本轮修改：商品=三文鱼poke
继承条件：日期=2026-05-01 至 2026-05-31，指标=净营业额
```

数字核对：

| 回答数字 | SQLite/API facts | 结论 |
|---:|---:|---|
| 净营业额 ¥30.00 | 30.0 | 一致 |
| 销量 1 份 | 1 | 一致 |
| 订单数 1 | 1 | 一致 |

## 数字为什么可信

### 1. 数字来自 SQLite，不由 LLM 计算

自然语言先被转换为受约束的查询计划，随后由后端白名单查询读取 `data/clean/sales_clean.sqlite`。营业额使用 `SUM(amount_clean)`，订单数使用 `COUNT(DISTINCT order_id)`；模型不能生成 SQL，也不能自行修改查询结果。

### 2. 使用独立 SQL 复算真值

自动化测试没有直接复用 AI 问答的聚合结果，而是单独连接 SQLite，根据日期、商品和门店重新执行聚合 SQL。测试要求：

```text
独立 SQL 结果 = API facts = 最终回答中的数字
```

上述三个用例全部满足该等式。完整验证报告共包含 5 个连续问答用例，`consistency_rate` 为 `1.0`，即 100%。

### 3. 回答数字需要通过白名单校验

后端会提取最终回答中的金额、数量和订单数，并检查每个数字是否存在于查询得到的 `facts` 中。如果模型写出事实集合之外的数字，整段模型回答会被丢弃，改用确定性模板。

测试曾主动注入错误回答“净营业额为 ¥999,999.00”，而 SQLite 真值为 `85.0`。系统成功移除了 `999,999` 并返回正确模板答案。这是故意构造的防护测试，不是真实线上事故。

### 4. 本次回答使用可信降级模板

验证报告中这三个用例的 `fallback_used` 均为 `true`，表示当时没有采用远程 LLM 自由生成的文本，而是使用 SQLite facts 和本地确定性模板组织答案。因此这里证明的是“AI 问答接口在模型不可用时仍能返回可信数字”，不是在声称远程模型本身永远不会出错。

## 复核入口

- 问答真值测试：`backend/tests/test_ai_truth_verification.py`
- 数字提取与校验：`backend/app/ai/answer.py`
- SQLite 事实查询：`backend/app/ai/facts.py`
- 本次验证报告：`data/reports/ai_answer_verification.json`

复核命令：

```powershell
$env:PYTHONPATH='backend'
.\backend\.venv\Scripts\python.exe -m pytest backend/tests/test_ai_truth_verification.py -q
```
